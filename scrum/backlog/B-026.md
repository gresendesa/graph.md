# B-026 - Rename project to MdBind and CLI command to mdb

- Status: done
- PO Priority: (pending definition)
- Risk: 2 (medium)
- Owner: gresendesa
- Creation date: 2026-06-06
- Last updated: 2026-06-06
- Dependencies: none (purely mechanical rename)

## Description

Rename the project from `mdgraph` to **MdBind** and the CLI entry point from
`mdgraph` to **`mdb`**. All documentation, source code, package metadata, and
tests must reflect the new names consistently.

## Scope

### Package and source

| File / Location | Change |
|-----------------|--------|
| `pyproject.toml` | `name = "mdgraph"` → `name = "mdbind"` |
| `pyproject.toml` | `description` — update |
| `pyproject.toml` | `[project.scripts]` entry: `mdgraph` → `mdb`, `mdgraph.cli:app` → `mdbind.cli:app` |
| `src/mdgraph/` directory | Rename to `src/mdbind/` |
| All `from mdgraph.X import Y` | → `from mdbind.X import Y` |
| `src/mdbind/cli.py` | `name="mdgraph"` in `Typer()` → `name="mdb"` |
| `src/mdbind/cli.py` | Help string references to `mdgraph` → `mdb` |
| `src/mdgraph.egg-info/` | Remove (will be regenerated) |

### Tests (14 files, ~39 occurrences)

| Pattern | Change |
|---------|--------|
| `from mdgraph.cli import app` | → `from mdbind.cli import app` |
| `from mdgraph.index import ...` | → `from mdbind.index import ...` |
| `from mdgraph.parser import ...` | → `from mdbind.parser import ...` |
| `from mdgraph.models import ...` | → `from mdbind.models import ...` |
| `from mdgraph.composer import ...` | → `from mdbind.composer import ...` |

### Documentation

| File | Change |
|------|--------|
| `README.md` | `mdgraph` → `MdBind` (title/brand), `mdb` (command), update all code examples |
| `specification.md` | All `mdgraph` references → `MdBind` / `mdb` |
| `CONSTITUTION.md` | Project name reference if present |
| `examples/README.md` | All `mdgraph` commands → `mdb` |
| `examples/wiki/*.md` | Any `mdgraph` references → `mdb` |
| `scrum/backlog.md` | Project name in header if present |

### Not in scope

- `scrum/backlog/B-XXX.md` and `scrum/sprint/SPR-YYYY-NN.md` — historical records,
  not updated per convention (old names in historical context are acceptable)
- Git history — not rewritten

## Acceptance criteria

- `mdb --help` works after reinstall
- `mdgraph` command no longer exists
- All Python imports use `mdbind`
- All documentation references the new names
- 209 tests pass (0 failures) after rename
- `pip install -e .` succeeds with the new package name

## Risk notes

- Risk is medium (2) because the rename touches every layer simultaneously
- No logic changes — purely mechanical
- Mitigation: use LSP rename + sed-based batch replacement where safe
